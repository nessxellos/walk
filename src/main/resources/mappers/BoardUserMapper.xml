<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.project.walk.dao.BoardUserMapper">
	<insert id="insert">
		INSERT INTO boarduservo (id, title, content, regdate, hitcnt, likecnt, writer)
		VALUES(seq_boarduservo.nextval, #{title}, #{content}, sysdate, #{hitCnt}, 1, #{writer})
	</insert>
	
	<select id="nextBoardNum" resultType="int">
		<![CDATA[
			SELECT NVL(MAX(bnum),0) + 1 AS bnum
			FROM boarduservo
		]]>
	</select>
	
	
</mapper>